global:
  resolve_timeout: 1m
route:
  receiver: n8n
  group_by: [alertname]
  group_wait: 2s
  group_interval: 10s
  repeat_interval: 5m
receivers:
  - name: n8n
    webhook_configs:
      - url: http://n8n:5678/webhook/prometheus-alert
        send_resolved: false
